import { DataslidetoDirective } from './dataslideto.directive';

describe('DataslidetoDirective', () => {
  it('should create an instance', () => {
    const directive = new DataslidetoDirective();
    expect(directive).toBeTruthy();
  });
});
